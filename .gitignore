node_modules
build
*.log
.DS_Store
.env
.env.local
coverage
.vite
*.tsbuildinfo

# Go build outputs
/solsclaw
/solsclaw-*
/dist-bin/

# UI build output, embedded at go build time. Keep .gitkeep so the //go:embed
# directive has a non-empty target on a fresh clone.
/web/dist/*
!/web/dist/.gitkeep
